<h1>Editing Post</h1>

<%= render partial: 'form', locals:{url: subject_post_path(@subject.id, @post.id), subjct_id: @subject.id } %>

<%= link_to 'Show', subject_post_path(@subject.id, @post.id) %> |
<%= link_to 'Back', subject_posts_path( @subject.id), subjct_id: @subject.id %>
